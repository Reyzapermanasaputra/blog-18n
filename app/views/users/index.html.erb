<h1>Users</h1>

<table class="table">
  <tr>
    <th>No</th>
    <th>Object</th>
  </tr>
<% @users.each_with_index do |user, index| %>
  <tr>
    <td><%= index+1 %></td>
    <td><%= user.email %></td>
  </tr>
  <% end %>
 </table>
 <h1><%= @users.unshift(2) %></h1>

